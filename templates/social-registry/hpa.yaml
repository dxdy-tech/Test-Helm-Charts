{{- if and .Values.socialRegistry.enabled .Values.socialRegistry.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "social-registry.fullname" . }}
  labels:
    {{- include "social-registry.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "social-registry.fullname" . }}
  minReplicas: {{ .Values.socialRegistry.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.socialRegistry.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.socialRegistry.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.socialRegistry.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.socialRegistry.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.socialRegistry.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
